package Algorithm;

public class DoubleLinkedNode {
	int val;
	      DoubleLinkedNode left;
	      DoubleLinkedNode right;
	      DoubleLinkedNode(int x) { val = x; }
	      
	public DoubleLinkedNode generateBTree(){
		DoubleLinkedNode root=new DoubleLinkedNode((int) Math.round((double)Math.random()*10));
		int i=(int) Math.round((double)Math.random()*10);
		
		
		
		
		return root;
	}
	 
}
