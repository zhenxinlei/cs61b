package Algorithm;

public class TreeNode {
	public  int val;
	public     TreeNode left;
	public     TreeNode right;
	      TreeNode(int x) { val = x; }
	      
	public TreeNode generateBTree(){
		TreeNode root=new TreeNode((int) Math.round((double)Math.random()*10));
		int i=(int) Math.round((double)Math.random()*10);
		
		
		
		
		return root;
	}
	 
}
